import { useQuery, useQueryClient } from "@tanstack/react-query";
import { Alert, Pressable, ScrollView, Text, View } from "react-native";
import { useRouter } from "expo-router";
import { useHouseholds } from "../households/household-context";
import { tripApi } from "../trips/api-client";
import { useShoppingListRealtime } from "../realtime/use-shopping-list-realtime";
export function ShoppingModeScreen({tripId}:{tripId:string}){const {selected}=useHouseholds();const qc=useQueryClient();const router=useRouter();const q=useQuery({queryKey:["households",selected?.id,"trips",tripId],queryFn:()=>tripApi.get(selected!.id,tripId),enabled:!!selected});const trip=q.data;useShoppingListRealtime(selected?.id,trip?.shopping_list_id,tripId);if(!trip)return <Text style={{padding:24}}>Loading trip…</Text>;const pending=trip.progress.pending;const act=async(fn:()=>Promise<unknown>)=>{await fn();await qc.invalidateQueries({queryKey:["households",selected?.id,"trips",tripId]});};const complete=()=>Alert.alert("Complete this shopping trip?","This trip cannot be reopened.", [{text:"Cancel",style:"cancel"},{text:"Complete",onPress:async()=>{await tripApi.complete(selected!.id,trip.id);await qc.invalidateQueries();router.replace("/purchases");}}]);return <ScrollView contentContainerStyle={{padding:24,paddingTop:70}}><Text style={{fontSize:30,fontWeight:"800"}}>Shopping mode</Text><Text style={{marginTop:8,fontSize:18}}>{trip.progress.collected} of {trip.progress.total} collected</Text>{pending>0&&<Text style={{marginTop:12,color:"#a33"}}>{pending} items still need attention</Text>}{trip.items.map(item=><View key={item.id} style={{padding:16,marginTop:12,borderRadius:14,backgroundColor:item.status==="collected"?"#d9f5df":"#fff"}}><Text style={{fontSize:18,fontWeight:"700"}}>{item.name}</Text><Text>{item.requested_quantity} {item.brand??""}</Text><View style={{flexDirection:"row",gap:8,marginTop:12}}>{item.status!=="collected"&&<Pressable onPress={()=>act(()=>tripApi.collect(selected!.id,trip.id,item.id))} style={{backgroundColor:"#245a43",padding:12,borderRadius:10}}><Text style={{color:"#fff"}}>Collected</Text></Pressable>}{item.status==="pending"&&<Pressable onPress={()=>act(()=>tripApi.skip(selected!.id,trip.id,item.id))} style={{backgroundColor:"#eee",padding:12,borderRadius:10}}><Text>Skip</Text></Pressable>}{item.status!=="pending"&&<Pressable onPress={()=>act(()=>tripApi.undo(selected!.id,trip.id,item.id))} style={{backgroundColor:"#eee",padding:12,borderRadius:10}}><Text>Undo</Text></Pressable>}</View></View>)}{pending===0&&<Pressable onPress={complete} style={{marginTop:20,backgroundColor:"#245a43",padding:16,borderRadius:12,alignItems:"center"}}><Text style={{color:"#fff",fontWeight:"700"}}>Complete shopping</Text></Pressable>}<Pressable onPress={()=>act(()=>tripApi.cancel(selected!.id,trip.id))} style={{marginTop:20,padding:15,alignItems:"center"}}><Text>End trip</Text></Pressable></ScrollView>}
