import { useState } from "react";
import { ActivityIndicator, Pressable, StyleSheet, Text, TextInput, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";
import { useHouseholds } from "../households/household-context";
export function CreateHouseholdScreen() { const { createHousehold } = useHouseholds(); const [name,setName]=useState(""); const [busy,setBusy]=useState(false); const [error,setError]=useState<string|null>(null); const submit=async()=>{if(!name.trim()){setError("Enter a household name.");return;}setBusy(true);setError(null);try{await createHousehold(name.trim());}catch{setError("Unable to create household.");}finally{setBusy(false);}}; return <SafeAreaView style={styles.screen}><View style={styles.content}><Text style={styles.title}>Create your household</Text><Text style={styles.body}>Start a shared space for shopping and planning.</Text><TextInput accessibilityLabel="Household name" value={name} onChangeText={setName} placeholder="e.g. The Kamau home" style={styles.input}/>{error?<Text>{error}</Text>:null}<Pressable disabled={busy} onPress={()=>void submit()} style={styles.button}>{busy?<ActivityIndicator color="#fff"/>:<Text style={styles.buttonText}>Create household</Text>}</Pressable></View></SafeAreaView>; }
const styles=StyleSheet.create({screen:{flex:1,backgroundColor:"#f7f4ed"},content:{flex:1,justifyContent:"center",padding:24},title:{fontSize:30,fontWeight:"800",color:"#15251b"},body:{fontSize:16,color:"#5b685f",marginVertical:12},input:{backgroundColor:"#fff",borderWidth:1,borderColor:"#b7c4bb",borderRadius:12,padding:14,fontSize:16},button:{marginTop:18,minHeight:52,alignItems:"center",justifyContent:"center",borderRadius:12,backgroundColor:"#245a43"},buttonText:{color:"#fff",fontWeight:"700",fontSize:16}});
