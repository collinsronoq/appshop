import { useQuery } from "@tanstack/react-query";
import { useRouter } from "expo-router";
import { ActivityIndicator, Pressable, StyleSheet, Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";
import { useHouseholds } from "../households/household-context";
import { householdApiClient } from "../households/api-client";
export function MembersScreen(){const router=useRouter();const {selected}=useHouseholds();const q=useQuery({queryKey:["household-members",selected?.id],queryFn:()=>householdApiClient.members(selected!.id),enabled:Boolean(selected)});return <SafeAreaView style={styles.screen}><View style={styles.content}><Text style={styles.title}>{selected?.name??"Members"}</Text>{q.isLoading?<ActivityIndicator/>:q.data?.map(m=><View key={m.user_id} style={styles.row}><View><Text style={styles.name}>{m.display_name}</Text><Text style={styles.email}>{m.email}</Text></View><Text style={styles.role}>{m.role}</Text></View>)}{selected?.role==="owner"?<Pressable onPress={()=>router.push("/invite")} style={styles.button}><Text style={styles.buttonText}>Invite member</Text></Pressable>:null}<Pressable onPress={()=>router.back()} style={styles.link}><Text>Back</Text></Pressable></View></SafeAreaView>}
const styles=StyleSheet.create({screen:{flex:1,backgroundColor:"#f7f4ed"},content:{padding:24},title:{fontSize:28,fontWeight:"800",marginBottom:18},row:{backgroundColor:"#fff",padding:14,borderRadius:12,marginBottom:8,flexDirection:"row",justifyContent:"space-between"},name:{fontWeight:"700"},email:{color:"#68756d",marginTop:3},role:{color:"#245a43",fontWeight:"700"},button:{marginTop:18,backgroundColor:"#245a43",padding:16,borderRadius:12,alignItems:"center"},buttonText:{color:"#fff",fontWeight:"700"},link:{alignItems:"center",padding:16}});
