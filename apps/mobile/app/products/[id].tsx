import Feather from "@expo/vector-icons/Feather";
import { useQuery, useQueryClient } from "@tanstack/react-query";
import { useLocalSearchParams, useRouter } from "expo-router";
import { Alert, Image, StyleSheet, Text, View } from "react-native";
import { AppScreen, BackHeader, InlineError, LoadingState, PrimaryButton, SecondaryButton, SurfaceCard } from "../../src/design/components";
import { colors, radius, spacing, typography } from "../../src/design/theme";
import { useHouseholds } from "../../src/households/household-context";
import { listApi } from "../../src/lists/api-client";
import { productApi } from "../../src/products/api-client";
import { purchasesApi } from "../../src/purchases/api-client";
export default function ProductDetail() { const { id } = useLocalSearchParams<{ id: string }>(); const router = useRouter(); const { selected } = useHouseholds(); const qc = useQueryClient(); const q = useQuery({ queryKey: ["households", selected?.id, "products", id], queryFn: () => productApi.get(selected!.id, id), enabled: Boolean(selected && id) }); const summary = useQuery({ queryKey: ["households", selected?.id, "products", id, "summary"], queryFn: () => purchasesApi.summary(selected!.id, id), enabled: Boolean(selected && id) }); const lists = useQuery({ queryKey: ["households", selected?.id, "shopping-lists"], queryFn: () => listApi.lists(selected!.id), enabled: Boolean(selected) }); if (q.isLoading) return <AppScreen><LoadingState rows={5} /></AppScreen>; if (q.isError || !q.data) return <AppScreen><InlineError onRetry={() => void q.refetch()} /></AppScreen>; const p = q.data; const add = () => { const list = lists.data?.[0]; if (list) router.push(`/lists/${list.id}/add?productId=${p.id}`); else router.push("/lists/new"); }; const archive = () => { if (!selected) return; Alert.alert(`Archive ${p.name}?`, "It will remain in purchase history but cannot be added to new shopping lists.", [{ text: "Cancel", style: "cancel" }, { text: "Archive", style: "destructive", onPress: async () => { await productApi.archive(selected.id, p.id); await qc.invalidateQueries({ queryKey: ["households", selected.id, "products"] }); router.back(); } }]); }; return <AppScreen><BackHeader title="Product detail" onBack={() => router.back()} right={<SecondaryButton label="Edit" fullWidth={false} icon="edit-2" onPress={() => router.push(`/products/edit?id=${p.id}`)} />} />{p.archived_at ? <Text style={styles.archived}>Archived</Text> : null}<View style={styles.hero}>{p.image_url ? <Image source={{ uri: p.image_url }} style={styles.image} /> : <Feather color={colors.primary} name="package" size={46} />}</View><Text style={styles.name}>{p.name}</Text>{p.brand ? <Text style={styles.brand}>{p.brand}</Text> : null}<Text style={styles.meta}>{[p.variant, p.size_value && `${p.size_value} ${p.size_unit ?? ""}`].filter(Boolean).join(" · ")}</Text>{p.category ? <Text style={styles.category}>{p.category.display_name}</Text> : null}<SurfaceCard style={styles.usual}><Feather color={colors.primary} name="shopping-cart" size={20} /><Text style={styles.usualText}>Usually buy {p.usual_quantity}</Text></SurfaceCard>{p.notes ? <><Text style={styles.section}>Notes</Text><Text style={styles.body}>{p.notes}</Text></> : null}<Text style={styles.section}>Preferred substitute</Text><SurfaceCard><Text style={styles.body}>{p.preferred_substitutes?.[0]?.substitute_name ?? "No preferred substitute set"}</Text></SurfaceCard><Text style={styles.section}>Purchase summary</Text><SurfaceCard style={styles.summary}><View><Text style={styles.meta}>Last purchased</Text><Text style={styles.body}>{summary.data?.last_purchased_at ? new Date(summary.data.last_purchased_at).toLocaleDateString(undefined, { month: "short", day: "numeric" }) : "Not purchased yet"}</Text></View><View><Text style={styles.meta}>Purchased</Text><Text style={styles.body}>{summary.data?.purchase_count ?? 0} time{summary.data?.purchase_count === 1 ? "" : "s"}</Text></View></SurfaceCard>{!p.archived_at ? <PrimaryButton label="Add to shopping list" icon="plus" onPress={add} /> : null}<SecondaryButton label="View purchase history" icon="clock" onPress={() => router.push(("/products/" + p.id + "/history") as never)} /><SecondaryButton label="Archive product" icon="archive" onPress={archive} /> </AppScreen>; }
const styles = StyleSheet.create({ hero: { height: 230, borderRadius: radius.lg, backgroundColor: colors.primarySubtle, alignItems: "center", justifyContent: "center", overflow: "hidden", marginBottom: spacing.lg }, image: { width: "100%", height: "100%" }, name: { ...typography.display, color: colors.text }, brand: { ...typography.body, color: colors.textSecondary, marginTop: spacing.xs }, meta: { ...typography.secondary, color: colors.textSecondary, marginTop: spacing.xs }, category: { alignSelf: "flex-start", ...typography.caption, color: colors.primary, fontWeight: "800", backgroundColor: colors.primarySubtle, borderRadius: radius.round, paddingHorizontal: spacing.md, paddingVertical: spacing.xs, marginTop: spacing.md }, usual: { flexDirection: "row", alignItems: "center", gap: spacing.sm, marginTop: spacing.lg }, usualText: { ...typography.bodyStrong, color: colors.primary }, section: { ...typography.sectionTitle, color: colors.text, marginTop: spacing.xl, marginBottom: spacing.sm }, body: { ...typography.body, color: colors.text }, summary: { flexDirection: "row", justifyContent: "space-between", marginBottom: spacing.lg }, archived: { alignSelf: "flex-start", color: colors.warning, backgroundColor: colors.warningSurface, borderRadius: radius.round, paddingHorizontal: spacing.md, paddingVertical: spacing.xs, marginBottom: spacing.md } });
