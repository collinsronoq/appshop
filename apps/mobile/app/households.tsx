import Feather from "@expo/vector-icons/Feather";
import { useRouter } from "expo-router";
import { Pressable, StyleSheet, Text, View } from "react-native";
import { AppScreen, BackHeader, EmptyState } from "../src/design/components";
import { colors, iconSizes, spacing, typography } from "../src/design/theme";
import { useHouseholds } from "../src/households/household-context";
export default function Households() { const router = useRouter(); const { households, selectedId, selectHousehold } = useHouseholds(); const choose = async (id: string) => { try { await selectHousehold(id); router.back(); } catch { /* keep current valid selection */ } }; return <AppScreen><BackHeader title="Switch household" onBack={() => router.back()} />{households.length ? households.map(h => <Pressable key={h.id} accessibilityRole="button" onPress={() => void choose(h.id)} style={[styles.row, h.id === selectedId ? styles.selected : null]}><View style={styles.icon}><Feather color={colors.primary} name="home" size={iconSizes.md} /></View><View style={styles.copy}><Text style={styles.name}>{h.name}</Text><Text style={styles.meta}>{h.role === "owner" ? "Owner" : "Member"} · {h.member_count} member{h.member_count === 1 ? "" : "s"}</Text></View>{h.id === selectedId ? <View style={styles.current}><Feather color={colors.primary} name="check" size={16} /><Text style={styles.currentText}>Current</Text></View> : <Feather color={colors.textSecondary} name="chevron-right" size={iconSizes.md} />}</Pressable>) : <EmptyState icon="home" title="No households yet" body="Create a household to start shopping together." />}</AppScreen>; }
const styles = StyleSheet.create({ row: { minHeight: 76, flexDirection: "row", alignItems: "center", gap: spacing.md, padding: spacing.md, backgroundColor: colors.surface, borderWidth: 1, borderColor: colors.border, borderRadius: 14, marginBottom: spacing.sm }, selected: { borderColor: colors.primary, backgroundColor: colors.primarySubtle }, icon: { width: 42, height: 42, borderRadius: 12, alignItems: "center", justifyContent: "center", backgroundColor: colors.primarySubtle }, copy: { flex: 1 }, name: { ...typography.cardTitle, color: colors.text }, meta: { ...typography.secondary, color: colors.textSecondary, marginTop: spacing.xs }, current: { flexDirection: "row", alignItems: "center", gap: 4 }, currentText: { ...typography.caption, color: colors.primary, fontWeight: "800" } });
